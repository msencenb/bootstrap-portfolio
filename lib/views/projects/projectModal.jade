.modal(id="#{project.pretty_id()}_modal",tabindex=-1,role="dialog",aria-labelledby="#{project.pretty_id()}_label",aria-hidden="true")
	.modal-header
		button.close(type="button",data-dismiss="modal") x
		h3(id="#{project.pretty_id()}_label") #{project.name}
	.modal-body
		div!= project.long_description
	.modal-footer
		button.btn(data-dismiss="modal") Close